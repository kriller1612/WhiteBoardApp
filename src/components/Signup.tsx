import {Auth} from 'firebase/auth'
import firebase from 'firebase/app';


export default function Signup(props: {auth: Auth}) {
    return (
        <div>
            <h1>Signup</h1>
        </div>
    )

}